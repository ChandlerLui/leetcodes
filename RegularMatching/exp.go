package RegularMatching

// too hard i cant solve problem like this maybe later
func isMatch(s string, p string) bool {
	//for _, ss := range s{
	//	if strings.HasPrefix(s, " "){
	//
	//	}
	//}
	return false
}
